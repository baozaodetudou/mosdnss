# 技术设计: mosdns demo 请求流程图

## 技术方案
### 核心技术
- Mermaid flowchart 语法 (graph LR + subgraph 分区)
- `npx @mermaid-js/mermaid-cli` 生成 SVG

### 实现要点
- 入口节点按照 `sequence_6666` 的开关顺序分组，标注默认值与结果。
- 使用子图区分“入口过滤”“主分流”“名单结果”“模式决策”，减少交叉连线。
- 以并行分支表达兼容/安全模式，并统一 fakeip/realip 终态标签。

## 架构设计
无新增架构，仅补充文档。

## 架构决策 ADR
无。

## API设计
无。

## 数据模型
无。

## 安全与性能
- **安全:** 无代码执行路径，本次仅生成静态图。
- **性能:** Mermaid CLI 本地运行一次，SVG 可直接提交。

## 测试与部署
- **测试:** 本地打开 SVG 验证节点完整性；使用 `npx mmdc` 重新生成确认可复现。
- **部署:** 将 `.mmd` 与 `.svg` 一同提交，供文档引用。
